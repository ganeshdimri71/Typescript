"use strict";
;
const logDetails = (uid, item) => {
    console.log(`${item} has a uid ${uid}`);
};
const greet = (user) => {
    console.log(`${user.name} says hello`);
};
const greetAgain = (user) => {
    console.log(`${user.name} says hello`);
};
const logDetailsOne = (uid, item) => {
    console.log(`${item} has a uid ${uid}`);
};
const greetOne = (user) => {
    console.log(`${user.name} says hello`);
};
const greetAgainOne = (user) => {
    console.log(`${user.name} says hello`);
};
